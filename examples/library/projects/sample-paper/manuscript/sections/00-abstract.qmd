# Abstract

**Scaling Attention**

Ada Lovelace, Alan Turing

We study how self-attention behaves as model and data scale jointly. Across a
sweep of architectures we find that attention entropy is a reliable, *training-free*
predictor of downstream transfer — tracking final accuracy more closely than
parameter count alone. The result suggests cheap, early signals for model
selection well before convergence.
